// Central debug flag for TravelButton project.
// Toggle DebugConfig.IsDebug = true (or bind to ConfigManager) to enable verbose logs.
public static class DebugConfig
{
    // Default false in production. You can set this at runtime from your mod config,
    // or set it to true temporarily while debugging.
    public static bool IsDebug = false;
}